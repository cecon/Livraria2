// Tela Cadastro de livro (US2). Placeholder — preenchido em incremento posterior.

export default function Cadastro() {
  return (
    <div className="p-6">
      <h1 className="text-2xl font-semibold tracking-tight">Cadastro</h1>
      <p className="text-muted-foreground mt-1 text-sm">
        Incluir / alterar / excluir livro — em construção (US2).
      </p>
    </div>
  );
}
