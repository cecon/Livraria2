// Tela Venda / PDV (US1) — tela principal. Placeholder — preenchido no MVP US1.

export default function Venda() {
  return (
    <div className="p-6">
      <h1 className="text-2xl font-semibold tracking-tight">Venda</h1>
      <p className="text-muted-foreground mt-1 text-sm">
        PDV — em construção (US1, MVP).
      </p>
    </div>
  );
}
